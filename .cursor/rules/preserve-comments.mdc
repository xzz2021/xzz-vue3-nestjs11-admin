---
description: 修改文件时保留已有注释，除非必要才删除
alwaysApply: true
---

# 保留注释

修改已有文件时，默认保留原有注释（行注释、块注释、JSDoc、TODO）。

不要为了「看起来更干净」或「顺手整理」而删除、改写、搬迁注释。改代码时优先改逻辑，不要清掉周围注释。

仅在以下情况可以删除或改写注释：

- 注释与代码已明显矛盾，继续留着会误导
- 注释描述的代码已被删除
- 用户明确要求清理注释

```typescript
// ❌ BAD：改逻辑时顺手删掉仍有效的注释
-  // 按组织范围过滤，避免越权看到其他租户客户
   const customers = await this.findByOrg(orgId);

// ✅ GOOD：保留原注释，只改需要改的代码
   // 按组织范围过滤，避免越权看到其他租户客户
   const customers = await this.findByOrg(orgId);
```
